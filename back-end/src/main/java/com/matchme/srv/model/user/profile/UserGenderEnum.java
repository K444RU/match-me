package com.matchme.srv.model.user.profile;

/**
 * Represents the user gender types as an enum. Used for better type safety and code clarity than
 * the previous entity approach.
 */
public enum UserGenderEnum {
  MALE,
  FEMALE,
  OTHER,
}
